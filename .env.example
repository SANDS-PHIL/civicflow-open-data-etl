# API Configuration for CivicFlow Open Data ETL
API_URL=https://jsonplaceholder.typicode.com/posts
# Fallback CSV path for when the API is unavailable
FALLBACK_CSV_PATH=data/fallback_facilities.csv
# API_KEY is not used for the mock API but kept for structure
API_KEY=your_api_key_here